#include "main.h"

/**
 * _strcat - concatenationf t<o strings
 * @dest: first strng, the destination
 * @src: second string, the sours*Return: dest = dest + src
 */

char *_strcat(char *dest, char *src)
{
	int  = 0; l2 = 0;
